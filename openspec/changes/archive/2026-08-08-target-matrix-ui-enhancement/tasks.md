## 1. Frontend UI Enhancements

- [x] 1.1 Update `VmMatrixGrid.jsx` to increase tile sizes (`h-14` / `h-16`), typography, and grid layout.
- [x] 1.2 Implement active node selection state (`selectedVm`) with visual highlight border in `VmMatrixGrid.jsx`.
- [x] 1.3 Upgrade the bottom detail inspector section in `VmMatrixGrid.jsx` to show full node metadata on click.
- [x] 1.4 Connect `selectedVm` state in `App.jsx` and re-test UI responsiveness.

## 2. Verification

- [x] 2.1 Rebuild frontend with `npm run build` and verify tile sizing and click detail rendering.
